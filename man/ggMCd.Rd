% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggMCd.R
\name{ggMCd}
\alias{ggMCd}
\title{Plot MCd Data with ggplot2}
\usage{
ggMCd(
  x,
  col = 1,
  fill = "#00000022",
  add = TRUE,
  scalefactor = 1,
  smax = FALSE,
  ylab = "Density",
  xlab = "Automatic",
  Toffset = 0,
  grid = FALSE,
  lwd = 1,
  lty = 1,
  sigma = 1,
  ...
)
}
\arguments{
\item{x}{A data frame or matrix where the first column represents x-values (time or other units)
and the remaining columns represent y-values (density or other measurements).}

\item{col}{Color for the mean line. Default is \code{1} (black).}

\item{fill}{Fill color for the shaded area representing standard deviation. Default is \code{'#00000022'}, pale transparant grey.}

\item{add}{Logical. If \code{TRUE} (default), returns a ggplot object. If \code{FALSE}, displays the plot.
so it can be added to an existing plot.}

\item{scalefactor}{Numeric. Factor to scale the y-values. Default is \code{1}.}

\item{smax}{Logical. If \code{TRUE}, rescales the y-values to a maximum of \code{1}. Default is \code{FALSE}.}

\item{ylab}{Character. Label for the y-axis. Default is \code{'Density'}.}

\item{xlab}{Character. Label for the x-axis. If set to \code{'Automatic'}, the function determines an appropriate
label based on \code{x} values (e.g., \verb{Cal. BC/AD}, \verb{Cal. BC}, \verb{Cal. AD}, or \verb{kyr BP}). Default is \code{'Automatic'}.}

\item{Toffset}{Numeric. Offset value added to the x-values. Default is \code{0}.}

\item{grid}{Logical. If \code{TRUE}, adds grid lines to the plot. Default is \code{FALSE}.}

\item{lwd}{Numeric. Line width for the mean line. Default is \code{1}.}

\item{lty}{Numeric or character. Line type for the mean line (e.g., \code{1} for solid). Default is \code{1}.}

\item{sigma}{Numeric. Multiplier for the standard deviation in the shaded area. Default is \code{1}.}

\item{...}{Additional arguments passed to \code{ggplot2} functions.}
}
\value{
If \code{add = TRUE}, returns a ggplot object. Otherwise, prints the plot.
}
\description{
This function replicates the functionality of the base R \code{plot.MCd} for plotting
MCd (Monte Carlo density) data, but uses \code{ggplot2} for alternative plotting syntax and aesthetics.
It plots the mean values of the density estimate, with an optional shaded area representing
the standard deviation.
}
\examples{
# Example usage (with some typical ggplot2-style additions):
dates <- data.frame(BP = c(4840, 4885, 4739, 4826, 4610), sd = c(45, 50, 27, 24, 31))
denmod <- MCdensity(dates)
plotmod <- ggMCd(denmod) + theme_classic()
plotmod
}
